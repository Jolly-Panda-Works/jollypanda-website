Reserved for future page imagery (Portfolio, Careers, Blog, etc).
Icons in this project are inline SVG in the HTML — this folder is not
currently used but is kept so the documented file structure stays stable
as new pages are added.
